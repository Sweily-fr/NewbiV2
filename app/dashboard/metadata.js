/**
 * Métadonnées pour le dashboard (pages privées)
 * Ces pages ne doivent PAS être indexées par les moteurs de recherche
 */
export const dashboardMetadata = {
  robots: {
    index: false,
    follow: false,
    nocache: true,
    noarchive: true,
    noimageindex: true,
  },
};
